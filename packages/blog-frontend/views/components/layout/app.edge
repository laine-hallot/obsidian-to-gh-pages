<!DOCTYPE html>
<html lang="en" class="bg-white dark:bg-neutral-900 dark:text-gray-100">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="app.css" rel="stylesheet">
    <title>{{title}}</title>
  </head>
  <body>
    @if ($slots.content)
      {{{ await $slots.content() }}}
    @endif
  </body>
</html>